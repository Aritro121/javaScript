/* let marks = {
    Aritro: 50,
    Raju: 21,
    Hinata: 42,
    Sasuka: 66,
    Naruto: 33
}

for(i=0; i<Object.keys(marks).length; i++){
    console.log("The mark of " + Object.keys(marks[i]) + "are" + marks[Object.keys(marks)[i]]);
} */

/* let marks = {
    Aritro: 50,
    Raju: 21,
    Hinata: 42,
    Sasuka: 66,
    Naruto: 33
}

for (let key in marks) {
    console.log("The mark of " + key + " is " + marks[key]);
}
 */

/* let marks = {
    Aritro: 20,
    Rana: 11,
    Hina: 44,
    Shimul: 11
}

let keys = Object.keys(marks);

for (let i = 0; i < keys.length; i++) {
    let key = keys[i];
    console.log("The mark of " + keys + " is " + marks[key]);
}

 */

/* let obj = {
    Aritro: 20,
    Tony: 21,
    Roger: 17,
    Garp: 17
}

for(let me in obj){
    console.log("The marks of " + me + "is " + obj[me] + '.' );
} */

/* let sum = {
    King: 30,
    Quine: 43,
    Prince: 21,
    Princese: 12
}

for(i=0; i< Object.keys(sum).length; i++){
    console.log("The mark is " + Object.keys(sum)[i] + " are " + sum[Object.keys(sum)[i]]);
} */


let she = {
    tina: 31,
    hina: 32,
    bina: 12,
    sakura: 33
}

for(i=0; i< Object.keys(she).length; i++){
    console.log("The mark is " + Object.keys(she)[i] + " are " + she[Object.keys(she)[i]]);
}